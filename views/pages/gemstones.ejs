<%- include('../layouts/main', { body: `

<!-- Gemstone Hero -->
<section class="gem-hero">
  <div class="container">
    <div class="gem-hero-content" data-aos="fade-up">
      <span class="section-label">Bộ Sưu Tập Đá Quý</span>
      <h1 class="gem-hero-title">Khoa Học Của<br><em>Sự Rực Rỡ</em></h1>
      <p class="gem-hero-subtitle">Khám phá thế giới phi thường của đá quý nhân tạo. Mỗi viên đá được tạo ra bằng công nghệ tiên tiến, mang lại vẻ đẹp và độ bền tương đương đá tự nhiên — một cách có đạo đức và bền vững.</p>
    </div>
    <div class="gem-hero-logo">
      <img src="/images/laclac-logo.jpg" alt="LacLac Jewelry" class="gem-logo-img">
    </div>
  </div>
</section>

<!-- Gemstone of the Month -->
${gemstones.filter(g => g.gemstoneOfMonth).map(g => `
<section class="gem-of-month">
  <div class="container">
    <div class="gem-month-grid">
      <div class="gem-month-visual" data-aos="fade-right">
        <div class="gem-month-image">
          <img src="${g.image}" alt="${g.name}" class="gem-month-real-img">
          <div class="gem-month-badge">✦ Đá Quý Của Tháng — ${g.monthFeatured}</div>
        </div>
      </div>
      <div class="gem-month-info" data-aos="fade-left">
        <span class="section-label">✦ Đá Quý Của Tháng</span>
        <h2 class="section-title">${g.name}</h2>
        <p class="gem-tagline">${g.tagline}</p>
        <p class="gem-text">${g.description}</p>
        <div class="gem-month-stats">
          <div class="gem-stat">
            <span class="gem-stat-label">Nguồn Gốc</span>
            <p>${g.origin}</p>
          </div>
          <div class="gem-stat">
            <span class="gem-stat-label">Ý Nghĩa</span>
            <p>${g.meaning}</p>
          </div>
        </div>
        <div class="gem-month-colors">
          <span class="gem-stat-label">Màu Sắc Có Sẵn</span>
          <div class="gem-color-tags">
            ${g.colors.map(c => '<span class="gem-color-tag">' + c + '</span>').join('')}
          </div>
        </div>
        ${g.relatedProducts.length > 0 ? `
          <div class="gem-month-cta">
            <a href="/product/${g.relatedProducts[0]}" class="btn btn-primary">Mua Trang Sức ${g.name}</a>
          </div>
        ` : ''}
      </div>
    </div>
  </div>
</section>
`).join('')}

<!-- All Gemstones Grid -->
<section class="gemstones-grid-section">
  <div class="container">
    <div class="section-header" data-aos="fade-up">
      <span class="section-label">Đá Quý Của Chúng Tôi</span>
      <h2 class="section-title">Khám Phá Từng Viên Đá</h2>
      <p class="section-desc">Từ độ rực rỡ vĩnh cửu của kim cương đến ánh lửa lấp lánh của ruby — tìm hiểu về những viên đá quý chúng tôi sử dụng.</p>
    </div>

    <div class="gemstones-grid">
      ${gemstones.map(g => `
        <div class="gem-card" data-aos="fade-up" id="${g.slug}">
          <div class="gem-card-image">
            <img src="${g.image}" alt="${g.name}" class="gem-card-real-img">
            ${g.featured ? '<div class="gem-featured-tag">Lựa Chọn Phổ Biến</div>' : ''}
          </div>
          <div class="gem-card-body">
            <h3 class="gem-card-name">${g.name}</h3>
            <p class="gem-card-tagline">${g.tagline}</p>
            <p class="gem-card-desc">${g.description}</p>

            <div class="gem-card-details">
              <div class="gem-detail-row">
                <span class="gem-detail-label">Đặc Tính</span>
                <p>${g.characteristics}</p>
              </div>
              <div class="gem-detail-row">
                <span class="gem-detail-label">Nguồn Gốc</span>
                <p>${g.origin}</p>
              </div>
              <div class="gem-detail-row">
                <span class="gem-detail-label">Ý Nghĩa</span>
                <p>${g.meaning}</p>
              </div>
              <div class="gem-detail-row">
                <span class="gem-detail-label">Hướng Dẫn Bảo Quản</span>
                <p>${g.care}</p>
              </div>
            </div>

            <div class="gem-card-colors">
              ${g.colors.map(c => '<span class="gem-color-tag">' + c + '</span>').join('')}
            </div>

            ${g.relatedProducts.length > 0 ? `
              <div class="gem-card-products">
                <span class="gem-detail-label">Mua Sản Phẩm</span>
                <div class="gem-product-links">
                  ${g.relatedProducts.map(slug => {
                    const prod = products.find(p => p.slug === slug);
                    return prod ? '<a href="/product/' + prod.slug + '" class="gem-product-link">' + prod.name + ' →</a>' : '';
                  }).join('')}
                </div>
              </div>
            ` : ''}
          </div>
        </div>
      `).join('')}
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="gem-cta-section">
  <div class="container">
    <div class="gem-cta-content" data-aos="fade-up">
      <img src="/images/laclac-logo.jpg" alt="LacLac Jewelry" class="gem-cta-logo">
      <h2>Chưa Tìm Được Viên Đá Hoàn Hảo?</h2>
      <p>Chúng tôi cung cấp đa dạng đá quý nhân tạo. Hãy chia sẻ ý tưởng của bạn và chúng tôi sẽ tìm viên đá hoàn hảo cho sản phẩm theo yêu cầu của bạn.</p>
      <div class="gem-cta-buttons">
        <a href="https://www.instagram.com/laclac.jewelry/" target="_blank" rel="noopener" class="btn btn-primary">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="2" width="20" height="20" rx="5"/><circle cx="12" cy="12" r="5"/><circle cx="17.5" cy="6.5" r="1.5"/></svg>
          Liên Hệ Qua Instagram
        </a>
        <a href="/" class="btn btn-outline">← Quay Lại Bộ Sưu Tập</a>
      </div>
    </div>
  </div>
</section>

`, pageTitle: 'Bộ Sưu Tập Đá Quý' }) %>
